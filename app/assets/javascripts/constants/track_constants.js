(function (root) {
  if (typeof root.TrackConstants === "undefined") {
    root.TrackConstants = {}
  }

  root.TrackConstants = {
    TRACK_RECEIVED: "TRACK_RECEIVED",
    TRACK_CREATED: "TRACK_CREATED"
  };

})(this);