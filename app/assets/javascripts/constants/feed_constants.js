(function (root) {
  if (typeof root.FeedConstants === "undefined") {
    root.FeedConstants = {}
  }

  root.FeedConstants = {
    USER_FEED_RECEIVED: "USER_FEED_RECEIVED",
    EXPLORE_FEED_RECEIVED: "EXPLORE_FEED_RECEIVED"
  };

})(this);