(function(root) {
  'use strict';

  if (typeof root.ErrorConstants === "undefined") {
    root.ErrorConstants = {};
  }

  root.ErrorConstants = {
    ERRORS_RECEIVED: "ERRORS_RECEIVED"
  };
}(this));