(function (root) {
  if (typeof root.SearchConstants === "undefined") {
    root.SearchConstants = {}
  }

  root.SearchConstants = {
    RESULTS_RECEIVED: "RESULTS_RECEIVED",
    OPTION_RECEIVED: "OPTION_RECEIVED"
  };

})(this);